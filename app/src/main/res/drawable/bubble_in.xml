<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- View background color -->
    <solid
        android:color="@color/textin" >
    </solid>

    <!-- The radius makes the corners rounded -->
    <corners
        android:bottomRightRadius="10sp" android:bottomLeftRadius="10sp"
        android:topLeftRadius="0sp" android:topRightRadius="10sp">
    </corners>

    <padding android:left="10dp"
        android:top="5dp"
        android:right="5dp"
        android:bottom="5dp"
        />
</shape>