<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- View background color -->
    <solid
        android:color="@color/textout">
    </solid>

    <!-- The radius makes the corners rounded -->
    <corners
        android:bottomRightRadius="10sp" android:bottomLeftRadius="10sp"
        android:topLeftRadius="10sp" android:topRightRadius="0sp">
    </corners>

    <padding android:left="5dp"
        android:top="5dp"
        android:right="10dp"
        android:bottom="5dp"
        />
</shape>